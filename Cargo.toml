[package]
name = "concerto"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
reqwest = { version = "0.13.4", default-features = false, features = ["blocking", "rustls"] }
safe_unzip = "0.1.6"
semver-php = "0.1.0"
serde_json = "1"
